NIVEL 3: UI
  - testes que dependem da máquina, pode ser SELENIUM

NIVEL 2: SERVICE
  - teste de APIS por exemplo, testes menores porem mais longos

NIVEL 1: UNIT
  - testes unitários TDD, geralmente são vários testes devido a quebra de métodos

DICAS

  1 - testes unitários:
    1.1 evite ruídos entre os testes, sem dependencias
    1.2 escolha melhores asserts para cada momento
    1.3 procure usar Mocks para chamadas externas, ou seja crie dados falsos

  2 - testes de integracao (service)
    2.1 verifique se os métodos funcionam em conjunto
    2.2 isole o máximo possivel dos ambientes

  3 - testes de aceitação (E2E/ui)
    3.1 - valide apenas o fluxo de funcionamento do projeto
